backend:
  name: github
  repo: belowzerobussiness-beep/Salon # Aapka sahi repo path
  branch: main
  site_domain: salon-two-rosy.vercel.app # Aapka sahi Vercel URL
  base_url: https://api.netlify.com # Netlify ka OAuth gateway
  auth_endpoint: auth

publish_mode: editorial_workflow

media_folder: "images" # Images yahan save hongi
public_folder: "/images"

collections:
  - name: "settings"
    label: "Website Content"
    files:
      - label: "General Settings"
        name: "general"
        file: "data.json"
        fields:
          - { label: "Title", name: "title", widget: "string" }
          - { label: "Description", name: "description", widget: "text" }
          - { label: "Price List", name: "prices", widget: "list", fields: [
              { label: "Service Name", name: "service", widget: "string" },
              { label: "Price", name: "price", widget: "string" }
            ]}
